package Stack.Queue.basics;

public class basicsOfStackQueue {

//	 1 ->import java.util.Stack;   these is the package of stack 
	
//	Stack is the DataType like we use int for 
//	the Integer variable. stacks is the variable name.
	
//	2 -> if we want to store text we use String datatype.
//	Stack<Object> stacks;

//3 ->	To Inititlaize the stack we use <variable_name> = new Stack<>(); Now the stack is ready to use.

//stacks = new Stack<>();
//Example:
//Stack<Integer> stack = new Stack<>();  //Example stack of Integer
	
//4 -> 	Methods in Stack Class
//	1 ->> push(e): Adds element e to the top of the stack.
//	<stack_name>.push(<varible_name>);     this is syntex of push
//	Example:
//
//	stacks.push(45);
	
//	2 ->> pop(): Removes and returns the top element from the stack. If the stack is empty, it throws an exception.
//	<stack_name>.pop();       			this is syntex of pop
//	Example:
//
//	 stacks.peek();
//	3 ->> peek():
//	Returns the top element of the stack, without removing. If the stack is empty, it throws an exception.
//
//	<stack_name>.peek();						this is sytex of peek
//	Example:
//	stacks.peek();
	
	
//	4 ->> size(): Returns the number of elements in the stack.
//	 <stack_name>.size();					this is sytex of size
//	Example:
//	stacks.size();
	
//	5 ->> empty():
//	Returns a Boolean indicating whether the stack is empty
//	<stack_name>.empty();					this is syntex of empty
//	Example:
//	stacks.empty();
}
